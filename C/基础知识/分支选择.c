1.范围值使用
if(){}
	else if(){}
		else{}

2.有固定值使用
switch(ch)
{
	case 'a'(1):
	case 'b':
	case 'c':
	default:
}

3. 所有循环语句可用,但switch不可以
continue : 执行该语句时,会跳过本次迭代的剩余部分,并开始下一轮迭代.(即不执行剩下的语句,继续下一轮循环.)
[向上跳转]

4.使用循环和switch语句可用 
break : 终止包含它的循环,并继续执行下一阶段.(即,跳出循环,执行下面的语句)
[向下跳转]