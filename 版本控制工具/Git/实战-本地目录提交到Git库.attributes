实战-本地目录提交到Git仓库
1）cd 到目录

2）把项目初始化： git init

3）把文件(目录)加入暂存区： git add 文件名(目录)

4）把项目提交到本地仓库： git commit -m "提交文字说明"

5）Github创建一个远程库： New repository 

6）本地终端关联远程库： git remote  add origin  git@github.com:用户名/库名.git 
(需配好ssh，参考第九点)

7）远程库代码与本地合并： git pull --rebase origin master

8）本地git同步到远程库： git push origin master


之后修改文件提交步骤

1）git add 文件名

2）git commmit -m "提交文字说明"

3）git push origin master